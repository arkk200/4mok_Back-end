type onlinePlayerInfo = {
    id: string,
    nickname: string,
    imgBase64: string
};

type onlinePlayerInfoWithId = {
    id: string,
    nickname: string,
    imgBase64: string
}

export { onlinePlayerInfo, onlinePlayerInfoWithId };